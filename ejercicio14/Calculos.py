from math import pow

class Calculos:
    def cuadrado_numero(numero):
        cuadrado = pow(numero,2)
        return cuadrado
    
    def cubo_numero(numero):
        cubo = pow(numero, 3)
        return cubo